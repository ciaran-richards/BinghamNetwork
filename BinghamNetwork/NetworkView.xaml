<UserControl x:Class="NetworkDisplay.NetworkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkDisplay"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Viewbox Stretch="Fill" SizeChanged="FrameworkElement_OnSizeChanged" x:Name="Viewbox">
            <Canvas Grid.Column="0" x:Name="mycanvas" Margin="3" Width="100" Height="100"/>
        </Viewbox>
    </Grid>
</UserControl>
